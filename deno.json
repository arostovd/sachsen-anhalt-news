{
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "tasks": {
    "build": "deno run --allow-read --allow-write build-index.ts",
    "test": "deno test",
    "check": "deno check build-index.ts build-index_test.ts && deno lint && deno fmt --check"
  },
  "fmt": {
    "exclude": ["ausgaben/", "index.html"]
  },
  "lint": {
    "exclude": ["ausgaben/", "index.html"]
  }
}
